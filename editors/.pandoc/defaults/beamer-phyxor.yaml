to: beamer
pdf-engine: lualatex
template: beamer.tex

include-in-header:
  - header-phyxor.tex

resource-path:
  - .
  - ${HOME}/.pandoc/resources

filters:
  - diagram.lua

# By default, highest heading level in the hierarchy that is followed immediately by content
# slide-level: 2

variables:
  aspectratio: 169
